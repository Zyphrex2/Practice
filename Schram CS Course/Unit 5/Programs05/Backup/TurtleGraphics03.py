# TurtleGraphics03.py
# This program has the exact same output as the
# previous two.  By using the <*> "wildcard" we 
# can <import> all of the <turtle> library commands 
# at once making the code even less "wordy".


from turtle import *

setup(800,600)
forward(100)
update()
done()
