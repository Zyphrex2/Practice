# TurtleGraphics02.py
# This program has the exact same output as the
# previous one.  By importing the individual
# <turtle> library command in this way, the
# rest of the code can be less "wordy".


from turtle import setup
from turtle import forward
from turtle import update
from turtle import done

setup(800,600)
forward(100)
update()
done()
